node_modules/
dist/
preview-dist/
coverage/
.wrangler/
.dev.vars
.env
*.log
.idea/
.DS_Store
packages/box/test/.smoke-tmp/
packages/control-plane/.managed-dist/
scratchpad/
*.tsbuildinfo

# local go build output (the box image builds a linux binary from source)
packages/box/gateway/box-gateway
/REPORT.txt

# per-deployment Worker config (npm run config -w packages/control-plane)
packages/control-plane/wrangler.toml

# generated by `wrangler types` from the per-deployment wrangler.toml above,
# so committing it made every self-hoster's `wrangler types` dirty the tree
packages/control-plane/worker-configuration.d.ts

# local regression tests (agent-run, credential-adjacent; never committed)
/tests/
