/**
 * The Tasks list page used to live here. Desktop now hosts list + open detail
 * tabs in `tasks-workspace.tsx` (same shell for `/tasks` and `/tasks/$taskId`).
 * Keep this file as a stable import path for routes and external callers.
 */
export { TasksWorkspace as TasksView } from './tasks-workspace';
