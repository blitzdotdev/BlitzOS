/**
 * Stable type surface for the generated `dist-standalone/embedded-prompt.ts`
 * (see `scripts/embed-standalone.mjs`). Used as the `types` condition of the
 * `./prompt-text` export so typecheck never depends on the generated file
 * existing.
 */
export const reviewPrompt: string;
