diff --git a/dist/acp-agent.js b/dist/acp-agent.js
index 3f5dc82..0b70a45 100644
--- a/dist/acp-agent.js
+++ b/dist/acp-agent.js
@@ -865,6 +865,12 @@ export class ClaudeAcpAgent {
                                     },
                                 });
                             }
+                            if (message.event.type === "message_delta" &&
+                                message.event.delta?.stop_reason === "end_turn" &&
+                                message.origin?.kind !== "task-notification" &&
+                                promptReplayed) {
+                                terminalResultReceived = true;
+                            }
                         }
                         for (const notification of streamEventToAcpNotifications(message, params.sessionId, this.toolUseCache, this.client, this.logger, {
                             clientCapabilities: this.clientCapabilities,
