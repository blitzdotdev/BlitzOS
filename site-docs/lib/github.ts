/**
 * Public source repository link, shared by the landing hero / closing CTA and
 * the site footer so the marketing surfaces never drift apart.
 */
export const GITHUB_REPO_URL = 'https://github.com/LodyAI/Lody';
