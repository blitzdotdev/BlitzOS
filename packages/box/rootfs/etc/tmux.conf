set -g focus-events on
# One tab is one session: the webapp tab strip is the status UI.
set -g status off
# Real scroll everywhere: without mouse mode, wheel input degrades to arrow
# keys inside alternate-screen apps (Claude Code flags it).
set -g mouse on
