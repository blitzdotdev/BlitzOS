**/node_modules
.git
.idea
# Never ship local secrets or dev clutter into the build context.
# (env.defaults is a tracked template and is NOT matched by these.)
.env
**/.env
scratchpad/
plans/
tests/
**/dist
# Adapter snapshots can contain committed dist output (notably DSH), so keep them whole.
!vendor/lody-adapters/**
vendor/lody-adapters/**/node_modules
tools/
