{
  "note": "GET /orgs/:id/usage, session-authed, for an active member of the org. seatLimit is null where no billing service is attached and no seat gate runs; vmLimit is never null, because orgs.vm_limit has always had a value.",
  "body": {
    "seatsUsed": 3,
    "seatLimit": 5,
    "vmsUsed": 2,
    "vmLimit": 20
  },
  "gatingOff": {
    "seatsUsed": 3,
    "seatLimit": null,
    "vmsUsed": 2,
    "vmLimit": 20
  }
}
