{
  "contentType": "application/json",
  "body": {
    "box_id": "box-fixture",
    "access_token": "",
    "refresh_token": "refresh-fixture"
  },
  "expect": {
    "valid": false,
    "canonicalKeys": ["box_id", "access_token", "refresh_token"],
    "reason": "response fields must be non-empty strings"
  }
}
